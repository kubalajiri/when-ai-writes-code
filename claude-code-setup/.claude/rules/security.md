# Security

## Credentials

- **Never commit** API keys, passwords, or secrets
- **Never commit** `.env` files or credentials files
- Use environment variables for sensitive configuration

## Dependencies

- **Do NOT add** dependencies without explicit approval, use only really important dependencies